import '../../root/root.dart';

// todo => Widget / box Linear

class BoxLinear extends StatelessWidget {
  const BoxLinear({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
